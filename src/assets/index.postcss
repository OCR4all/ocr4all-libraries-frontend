@tailwind base;
@tailwind components;
@tailwind utilities;

.router-link-active:not(.unstyled) {
  @apply rounded-md bg-primary-600 bg-gradient-to-r from-primary-600 to-primary-600 font-semibold text-white dark:bg-gradient-to-r dark:from-primary-600 dark:to-primary-600;
  svg {
    @apply font-semibold text-white;
  }
}

.baklava-input {
  @apply dark:!placeholder-black dark:!placeholder-opacity-80;
}
